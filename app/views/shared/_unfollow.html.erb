<%= button_to relationship_path(relationship),
  method: :delete,
  class: "btn btn-sm btn-secondary active card-link",
  params: unfollow_params do %>
    <%= content_tag(:i, " #{count}", class: icon) %>
<% end %>
