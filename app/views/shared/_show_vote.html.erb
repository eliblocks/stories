<%= button_to vote_path,
              params: { vote_type: type, story_id: object.id },
              class: "btn btn-show btn-secondary #{toggle}" do %>
                <%= content_tag(:span, "",  class: icon) %>
                <%= content_tag(:span, "#{label}", class: "button-text") %>
                <%= content_tag(:span, "#{count}", class: "button-text") %>
              <% end  %>